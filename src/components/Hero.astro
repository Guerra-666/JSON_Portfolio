---
import { basics } from '@cv';
const {name, label, image, location, email, phone, summary} = basics;
const {city, region} = location;
---


<section>
    <div class="container">
        <div class="info">
            <h1>{name}</h1>
            <h2>{label}</h2>
    </div>

<h2>Hero</h2>